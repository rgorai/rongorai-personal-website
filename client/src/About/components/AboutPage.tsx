import { Link } from 'react-router-dom'
import styles from '../styles/aboutPage.module.scss'

interface Props {}

const AboutPage = (props: Props) => {
  return <div className={styles.aboutPageContainer}>About Page</div>
}

export default AboutPage
